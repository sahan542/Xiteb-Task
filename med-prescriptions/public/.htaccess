# public/.htaccess

# Disable directory listing
Options -Indexes

# Prevent executing PHP inside uploads/prescriptions
<IfModule mod_authz_core.c>
  <FilesMatch "^uploads/prescriptions/.*\.(php|phtml|phar|cgi|pl|asp|aspx)$">
    Require all denied
  </FilesMatch>
</IfModule>
